(: Using arrays and its native `map` function
(: `map`, `reduce` and `each` are the available functions for the Array type

times = |n| => {
  a = [1,2,3,4,5]
  return(a.map(|el| => {
    return(el * n)
  }))
}

puts(times(2)) (: [2, 4, 6, 8, 10]
