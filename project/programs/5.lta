(: Functions as first-class citizens implemented in LTA language :)
(: TODO: Check que onda fns sin params
applyToString = |a, b| => {
  return(puts(b(a)))
}

applyToString("Hi frIenD", |str| => {
  return(str.lower());
})
