(: Native asynchronic procedures

(: This is a synchronic function
sync = |x| => {
  i = 0
  loop(i < 1000000) {
    1 == 1
  }
}

(: This is an asynchronic function
async = |x| |> {
  sync(x)
}

(: TODO: Check the best way to test the asynchronicity
async("SYNC")
puts("HOLA :)")
