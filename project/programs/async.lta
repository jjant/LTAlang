(: Native asynchronic procedures

sync = |x| => {
  i = 0
  loop(i < 1000000) {
    1 == 1
  }
}

async = |x| |> {
  sync(x)
}

(: TODO: Check the best way to test the asynchronicity
async("SYNC")
puts("HOLA :)")
